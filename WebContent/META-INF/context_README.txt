Considering that I've used MySQL (phpMyAdmin WebUI), in context you can set up conection 
	to the DB as follows:

jdbc:mysql://localhost:3306/NAME_OF_DB?useUnicode=true&characterEncoding=utf8&"

If only that line is changed, and DB is of appropriate structure, everything should work just fine!

	Imposer out ^_^
